# 신시(神屍) — 프로젝트 규칙

찢긴 전쟁신 치우의 몸 위에 세운 도시에서, 번지는 저주를 막으며 형제들을 해방하는 탑다운 핵앤슬래시 ARPG.

## 먼저 읽을 문서

1. `docs/00-onepager.md` — 코어 루프, 기술 리스크(R1~R5), Go/No-Go 기준. **다구역 저주 시스템은 아직 미검증.**
2. `docs/01-design-bible.md` — 세계관, 직업, 저주 단계, 적, 아이템. 수치는 전부 프로토타입의 실제 값과 일치시켜 둔다.
3. `docs/02-art-guide.md` — 픽셀 아트 규격, 36색 팔레트, 청동기 유물 고증, 월드 단위 환산표(11.1).
4. `docs/sinsi-mudang-v5.html` — **현재 프로토타입.** v4에서 무당·적 6종·보스·장애물 3종을 `02-art-guide.md`가 적어둔 실루엣(무복 소매+방울+부채, 망령 소복, 돌진귀 뿔, 동두 족쇄 조각 등)대로 Canvas 도형을 더 정교하게 다시 그렸다. 수치·로직은 v4와 동일, 그리기 코드만 교체.
   `docs/sinsi-mudang-v4.html` — 원본 맛보기(수치 기준 원본으로 보관, 더는 참고용 비주얼 아님).

세 문서 다 자체 변경 이력을 갖고 있다. **수치나 방향을 바꾸면 코드보다 문서를 먼저 고치고, 변경 이력에 한 줄 남긴다.**

## 지금 상태 (2026-09-21)

- 완료: 무당 1직업 + 구역 1개 맛보기 프로토타입 (재미 검증)
- 미검증: 다구역 저주 배분 + 영구 전화(轉化). 이게 이 게임의 핵심 훅이자 최대 기술 리스크 (`docs/00-onepager.md` 8장 R1~R5)
- **다음 목표는 폼 좋은 단일 구역이 아니라, 구역 2~3개 + 전화 1회가 실제로 영속되는 작은 수직 슬라이스다.** R1~R5를 통과해야 본 개발(아트·사운드 전면 제작)에 들어간다.

## 원칙

### 1. 수치는 코드에 쓰지 않는다

모든 밸런스 수치(적 체력·속도·피해, 스킬 수치, 아이템 능력치 범위, 저주 상승률 등)는 `data/`의 JSON에서 읽는다. 코드에 숫자를 박아 넣지 않는다. 밸런싱을 코드 수정 없이 JSON만 고쳐서 할 수 있어야 한다 — 이게 안 되면 그 시점에 이 프로젝트는 설계 원칙을 어긴 것이다.

```
data/
  enemies.json         # 적 스펙, 프로토타입 값은 01-design-bible.md 6.1
  upgrades.json        # 레벨업 강화 13종, 01-design-bible.md 4.3
  curse.json           # 저주 단계·상승률, 01-design-bible.md 5.2
  items/
    affixes.json        # 추가 능력치 13종, 01-design-bible.md 7.3
    legendaries.json     # 전설 8종, 01-design-bible.md 7.4
```

값을 옮길 때는 `docs/02-art-guide.md` 11.1의 68px→32px 환산표를 따른다 (이동 속도·사거리·범위 계열만 ×0.47, 시간·확률·개수는 그대로).

### 2. 저주는 되돌릴 수 없다

구역이 "전화(轉化)"되면 그 구역은 놀이터로 되돌아가지 않는다. 이 규칙을 편의를 위해 완화하는 코드(예: 디버그 리셋 외의 되돌리기 기능)를 넣지 않는다. 세이브도 이 전제로 설계한다 — 전체 상태 저장이 아니라 **시드 + 변경분(delta)만** 저장한다 (`docs/00-onepager.md` R2).

### 3. 완전 자동사냥 없음 (확정, `01-design-bible.md` 9장)

이동·전투 판단은 항상 플레이어가 한다. "자동 방울"(반자동 공격)과 "부르지 않아도"(자동 초혼 빌드 선택지)는 예외로 유지한다. 그 외 자동 전투·시간 교환형 방치 시스템은 넣지 않는다.

### 4. 문화적으로 신중하게 다룬다

- 치우 배경은 "한민족의 역사"가 아니라 "신화에서 영감받은 판타지"로 소개한다 (`01-design-bible.md` 1.3).
- 적 제국의 수장을 실제 신화 속 인명(헌원 등)으로 명명하지 않는다.
- 무당은 조롱·공포 대상이 아니라 공동체를 지키는 사람으로 그린다. 실제 신당·무속인을 그대로 베끼지 않는다 (`docs/02-art-guide.md` 5.3).

## 폴더 구조

```
sinsi/
  CLAUDE.md              # 이 문서
  docs/
    00-onepager.md
    01-design-bible.md
    02-art-guide.md
    sinsi-mudang-v4.html # 최초 맛보기 (원본 보관용)
    sinsi-mudang-v5.html # 현재 프로토타입 (비주얼 개선판)
    systems/
      curse.md            # 다구역 저주 시스템 상세 명세 (예정, 00-onepager.md 11장)
  data/                  # 예정 — 모든 밸런스 수치의 정본
  assets/
    sprites/ tiles/ fx/ icons/ ui/   # 예정, 이름 규칙은 02-art-guide.md 11장
  src/                   # 예정 — Phaser 프로젝트
```

체크된 항목과 남은 항목은 `docs/01-design-bible.md` 11.1 클로드 코드 인계 체크리스트를 본다.

## 에셋 이름 규칙 (`docs/02-art-guide.md` 11장)

`종류_이름_동작_방향.png` — 예: `char_mudang_walk_down.png`, `enemy_ghoul_walk.png`, `tile_curse_02.png`. 기준점은 발끝 중앙. Phaser는 `pixelArt: true`, 카메라 배율은 정수만.
